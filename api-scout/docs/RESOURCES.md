# Resources Reference

API Scout currently focuses on **Tools** for active API interaction and does not expose static **Resources** at this time.

Future versions may expose:
- `api://docs/{id}`: Read-only access to stored spec files.
- `api://logs`: Access to recent API call logs.
